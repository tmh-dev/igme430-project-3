import { boardConstants } from '../constants';

export const boards = (state: any, action: any) => {
    switch (action.type) {
        case boardConstants.CREATE_SUCCESS:
            return ;
    }
}